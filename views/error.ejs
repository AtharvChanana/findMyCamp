<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Find My Camp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            padding-top: 2rem;
        }
        .error-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 2rem;
        }
    </style>
</head>
<body>
    <div class="container error-container">
        <div class="card shadow">
            <div class="card-body text-center p-5">
                <h1 class="display-1 text-danger mb-4"><%= statusCode || 500 %></h1>
                <h2 class="h3 mb-4"><%= message || 'Something went wrong!' %></h2>
                <p class="lead text-muted mb-4">
                    <%= statusCode === 404 ? 'The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.' : 'We are working on fixing the issue. Please try again later.' %>
                </p>
                <div class="d-flex justify-content-center gap-3 flex-wrap">
                    <a href="/" class="btn btn-primary">
                        <i class="fas fa-home me-2"></i>Back to Home
                    </a>
                    <a href="/campgrounds" class="btn btn-outline-secondary">
                        <i class="fas fa-campground me-2"></i>Browse Campgrounds
                    </a>
                </div>
                <% if (process.env.NODE_ENV === 'development' && stack) { %>
                    <div class="mt-4">
                        <button class="btn btn-sm btn-outline-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#errorDetails">
                            Show Error Details
                        </button>
                        <div class="collapse mt-3" id="errorDetails">
                            <pre class="bg-dark text-light p-3" style="max-height: 300px; overflow: auto; font-size: 0.8rem; border-radius: 0.25rem;">
<code><%= stack %></code>
                            </pre>
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<style>
.error-content {
    padding: 2rem;
    border-radius: 0.5rem;
    background-color: #fff;
    box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.05);
}

.display-1 {
    font-size: 6rem;
    font-weight: 700;
    line-height: 1;
    margin-bottom: 1rem;
}

.btn {
    padding: 0.5rem 1.5rem;
    border-radius: 50px;
    font-weight: 500;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
}

.btn i {
    transition: transform 0.2s ease;
    margin-right: 0.5rem;
}

.btn:hover i {
    transform: translateX(3px);
}

@media (max-width: 576px) {
    .display-1 {
        font-size: 4rem;
    }
    
    .btn {
        width: 100%;
        margin-bottom: 0.5rem;
    }
    
    .d-flex {
        flex-direction: column;
        gap: 0.5rem !important;
    }
}
</style>