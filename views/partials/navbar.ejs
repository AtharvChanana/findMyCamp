<nav class="navbar navbar-expand-lg navbar-dark bg-dark-2 py-2 py-lg-3 sticky-top" style="backdrop-filter: blur(10px); background: rgba(18, 18, 18, 0.8) !important;">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center me-4 me-lg-5" href="/" style="font-weight: 700; font-size: 1.75rem;">
            <i class="fas fa-map-marked-alt me-2 text-primary"></i>
            <span class="d-none d-sm-inline">find</span><span class="text-primary">MyCamp</span>
        </a>
        
        <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" 
                data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" 
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item me-2 me-lg-3">
                    <a class="nav-link<%= currentPath === '/' ? ' active' : '' %>" 
                       href="/" 
                       style="font-weight: 500; padding: 0.5rem 0.75rem;">
                        <i class="fas fa-home me-1"></i> Home
                    </a>
                </li>
                <li class="nav-item me-2 me-lg-3">
                    <a class="nav-link<%= currentPath.startsWith('/campgrounds') && !currentPath.endsWith('/new') && currentPath.split('/').length <= 2 ? ' active' : '' %>" 
                       href="/campgrounds"
                       style="font-weight: 500; padding: 0.5rem 0.75rem;">
                        <i class="fas fa-campground me-1"></i> Campgrounds
                    </a>
                </li>
                <li class="nav-item me-2 me-lg-3">
                    <a class="nav-link<%= currentPath === '/campgrounds/new' ? ' active' : '' %>" 
                       href="/campgrounds/new"
                       style="font-weight: 500; padding: 0.5rem 0.75rem;">
                        <i class="fas fa-plus-circle me-1"></i> Add Campground
                    </a>
                </li>
                <% if (currentUser) { %>
                <li class="nav-item me-2 me-lg-3">
                    <a class="nav-link<%= currentPath === '/campgrounds/saved' ? ' active' : '' %>" 
                       href="/campgrounds/saved"
                       style="font-weight: 500; padding: 0.5rem 0.75rem;">
                        <i class="fas fa-heart me-1"></i> Saved
                    </a>
                </li>
                <% } %>
            </ul>
            
            <div class="d-flex align-items-center ms-lg-4">
                <!-- Search Bar -->
                <form class="d-none d-lg-flex me-4" action="/campgrounds" method="GET" role="search">
                    <div class="input-group" style="width: 280px;">
                        <input class="form-control bg-dark border-secondary text-white" 
                               type="search" 
                               name="search"
                               placeholder="Search campgrounds..." 
                               aria-label="Search"
                               style="border-radius: 50px 0 0 50px; border-right: none;">
                        <button class="btn btn-primary px-3" type="submit" style="border-radius: 0 50px 50px 0;">
                            <i class="fas fa-search"></i>
                        </button>
                    </div>
                </form>
                
                <% if (currentUser) { %>
                <!-- User Dropdown -->
                <div class="dropdown ms-2">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" 
                       id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false"
                       style="padding: 0.5rem 0.75rem; margin-right: -0.5rem;">
                        <div class="position-relative">
                            <div class="rounded-circle bg-primary d-flex align-items-center justify-content-center" 
                                 style="width: 40px; height: 40px;">
                                <i class="fas fa-user"></i>
                            </div>
                        </div>
                        <span class="ms-2 d-none d-lg-inline" style="font-weight: 500;">
                            <%= currentUser.username.split(' ')[0] %>
                        </span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userDropdown" style="min-width: 220px;">
                        <li>
                            <a class="dropdown-item d-flex align-items-center" href="/campgrounds/saved">
                                <i class="fas fa-bookmark me-2 text-primary"></i> Saved Campgrounds
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item d-flex align-items-center" href="/users/<%= currentUser._id %>/reviews">
                                <i class="fas fa-star me-2 text-warning"></i> My Reviews
                            </a>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item d-flex align-items-center text-danger" href="/logout">
                                <i class="fas fa-sign-out-alt me-2"></i> Logout
                            </a>
                        </li>
                    </ul>
                </div>
                <% } else { %>
                <!-- Login/Register Links -->
                <div class="d-flex gap-3">
                    <a href="/login" class="btn btn-outline-light px-3" style="border-radius: 50px; min-width: 80px;">
                        Login
                    </a>
                    <a href="/register" class="btn btn-primary px-4" style="border-radius: 50px; min-width: 100px;">
                        Sign Up
                    </a>
                </div>
                <% } %>
                
                <!-- Mobile Search Toggle -->
                <button class="btn btn-link d-lg-none ms-2 text-decoration-none text-white" 
                        type="button" 
                        data-bs-toggle="collapse" 
                        data-bs-target="#mobileSearch" 
                        aria-expanded="false" 
                        aria-controls="mobileSearch"
                        style="padding: 0.5rem;">
                    <i class="fas fa-search fs-5"></i>
                </button>
            </div>
        </div>
        
        <!-- Mobile Search Bar -->
        <div class="collapse w-100 mt-2" id="mobileSearch">
            <form class="mb-2" action="/campgrounds" method="GET" role="search">
                <div class="input-group">
                    <input class="form-control bg-dark border-secondary text-white" 
                           type="search" 
                           name="search"
                           placeholder="Search campgrounds..." 
                           aria-label="Search">
                    <button class="btn btn-primary px-3" type="submit">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
            </form>
        </div>
    </div>
</nav>